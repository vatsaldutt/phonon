# secrets.yaml — fill in your real values, then keep this file private.
# This file sits next to esp32-proxy.yaml and is read by ESPHome at flash time.

wifi_ssid: "SpectrumSetup-8265"
wifi_password: "routinewinter686"

# Generate a random 32-byte base64 key for encrypting the HA ↔ ESP32 link.
# Run this in your terminal to generate one:
#   python3 -c "import base64, os; print(base64.b64encode(os.urandom(32)).decode())"
# Paste the output below (keep the quotes).
api_encryption_key: "wIvqylGkt5qAemATrIUXLSVBfpSOBMNiL13XafPDNbg="
